# Tensor Cores / Matrix Cores, numeric formats, precision emulation, matrix units for non-GEMM scientific kernels — GPU relevance verdict ledger

cluster: `F` (taxonomy F, with G overlap)
last_checked: 2026-09-18
knowledge_as_of: 2026-09-18

Verdicts are `CORE_GPU` / `RELATED_GPU` / `EXCLUDE` / `UNRESOLVED`.
Access states are `PUBLIC_FULLTEXT` / `CLOSED_ACCESS` / `PENDING_FULLTEXT` /
`PUBLIC_ARTIFACT_ONLY` / `ABSTRACT_ONLY`.
`evidence_read` records what was actually read — a verdict reached from an
abstract says `ABSTRACT_ONLY` and may not be used to justify a deep analysis.

**Counterfactual answer** column records the answer to: "If a generic
accelerator or a CPU were used instead, would the core contribution be
substantially the same?" `NO` = the contribution depends on a named
GPU-specific property. `NO (title/census-level)` = the named mechanism
(2:4 SpTC metadata, mma fragment shape, MFMA, FP8/MX format handling) is
evident from the confirmed title/abstract but the paper was not read, so the
answer is recorded as provisional.

**Tooling constraints that shaped access states** (do not re-derive):
`dl.acm.org` → 403, `ieeexplore.ieee.org` → 418, `dblp.org` → robots-blocked,
`arxiv.org/search` / `export.arxiv.org` / `web.archive.org` → unavailable.
Several SC 2025 / ISCA 2025 / MICRO 2025 papers in this cluster are
**genuinely open access** (some CC BY) but unreachable from this environment;
those are recorded `PENDING_FULLTEXT`, **not** `CLOSED_ACCESS`, with the
reason stated in the rationale.

---

## 1. Priority papers

| Paper | Venue/Year | Access state | Evidence read | Counterfactual answer | Verdict | Rationale (1-2 sentences) | Deep analysis file |
|---|---|---|---|---|---|---|---|
| ConvStencil: Transform Stencil Computation to Matrix Multiplication on Tensor Cores | PPoPP 2024 (Best Paper) | `PUBLIC_FULLTEXT` — author/institution PDF at `microsoft.com/en-us/research/wp-content/uploads/2024/04/ppopp24_ConvStencil.pdf` (DOI 10.1145/3627535.3638476; `dl.acm.org` 403). arXiv 2310.16298 **checked and ruled out** as a different paper (vector outer products, simulator eval). | `FULL_PAPER` — motivation, §2.3 three challenges, §3 design, Eq. 5-6/14 models, evaluation, 4-way performance breakdown, related work. No limitations section exists. No code (`NOT_INSPECTED`). | **NO** — A100's single FP64 MMA shape `m8n8k4`; dual tessellation fills 7/8 of the n extent; shared-memory capacity and bank mapping. | `CORE_GPU` | Stencil→GEMM lowering co-designed against a fixed FP64 fragment shape and an SM's banked shared memory; the measured breakdown shows the *layout* work, not the Tensor Core alone, is the dominant cause. | `GPU-PPoPP24-01--convstencil-stencil-to-matmul-tensor-cores.md` |
| SPIDER: Unleashing Sparse Tensor Cores for Stencil Computation via Strided Swapping | PPoPP 2026 | `PUBLIC_FULLTEXT` **with a caveat** — arXiv 2506.22035 (v1 2025-06-27, v2 2025-07-07, v3 2025-12-15; DOI 10.1145/3774934.3786414). `/abs/` shows the SPIDER title, but `arxiv.org/html/2506.22035v3` serves the **SPTCStencil-titled** text and the string "SPIDER" does not occur in it (verified). Same headline numbers in both abstracts. | `FULL_PAPER (of the SPTCStencil-titled text)` — §2.3 redundancy quantification, §3.2.2 three-step sparsification, kernel/tiling/packing, evaluation, related work. **No ablation, no limitations section.** No code (`NOT_INSPECTED`). | **NO** — the 2:4 structured-sparsity metadata contract; `L = 2r+2` and the `j ↔ j+(2r+2)` column swap exist only to satisfy "≤2 non-zeros per 4-element group"; `mma.sp m16n8k16` per-thread `.f16x2` register layout. | `CORE_GPU` | Reframes the padding zeros that prior TC-stencil work minimises as exploitable 2:4 sparsity that the Sparse Tensor Core skips in hardware; permutation folded into the shared→register load offset. | `GPU-PPoPP26-01--spider-sptcstencil-sparse-tensor-cores-stencil.md` |
| Double-Precision Matrix Multiplication Emulation via Ozaki-II Scheme with FP8 Quantization | SC 2026 (`MEMBERSHIP_UNVERIFIED`) | `PUBLIC_FULLTEXT` + `PUBLIC_ARTIFACT` — arXiv 2603.10634v1 (HTML for §I-IV, PDF for §IV-VI); artifact `github.com/RIKEN-RCCS/GEMMul8` cloned at commit `e5db07fa0aa69435d1319d3e56b4c25e9a68d625`. | `FULL_PAPER` + `CODE` — §I-A, §II, §III incl. §III-F, §IV model, §V results (6 platforms, accuracy, workspace), §VI conclusion. Code: moduli tables, E4M3 hole handling, k-blocking, backend dispatch, HIP FNUZ branch. | **NO** — FP8_E4M3 exact-integer range and representational holes; FP32 (not INT32) MMA accumulation forcing `k ≤ 2^16`; the INT8-vs-FP8 matrix-throughput asymmetry on Blackwell-Ultra-class parts (150 TOP/s vs 4500 TFLOP/s on B300 per the paper's Table I). | `CORE_GPU` | The paper exists because GPU vendors are withdrawing INT8 matrix throughput in favour of FP8; its content is entirely FP8 MMA operand/accumulator semantics. Note it calls a vendor GEMM rather than hand-writing MMA — GPU-specificity is *format*-driven. | `GPU-SC26-01--ozaki-ii-fp8-double-precision-gemm-emulation.md` |
| EmuGEMM: Fused Tensor Core Kernels for Precision Emulation in Matrix Multiplication | SC 2026 (`MEMBERSHIP_UNVERIFIED`) | `PUBLIC_FULLTEXT` — arXiv 2606.25453v1 (24 Jun 2026), HTML. No artifact located (`NOT_INSPECTED`). | `FULL_PAPER (§V-G limitations PARTIALLY_READ)` — motivation, §III-A interleaved layout, §III-B persistent kernel, §IV fused modular reduction + CRT-3M, §V setup/results/implementation, §VI related work. | **NO** — `wgmma.mma_async` (SM90, accumulators in the register file, tM=64/tK=32) vs `tcgen05.mma` (SM100, accumulators in **TMEM**, tM=128/tK=32); INT8×INT8→INT32 accumulate type making modular reduction fusable; in-register shift-reduce epilogue. | `CORE_GPU` | Turns Ozaki-scheme emulation from a sequence of vendor GEMM launches into one fused warpgroup kernel; the O(p²)→O(p) operand-load result is a direct consequence of matrix-unit accumulator placement. | `GPU-SC26-02--emugemm-fused-tensor-core-precision-emulation.md` |
| Avant-Garde: Empowering GPUs with Scaled Numeric Formats | ISCA 2025 | `PENDING_FULLTEXT` — DOI 10.1145/3695053.3731100. **The paper is open access (CC BY) but unreachable from here**: `dl.acm.org/doi/pdf/...` → 403 (attempted, confirmed); the EPFL Infoscience record `handle/20.500.14299/251538` confirms an open-access CC BY PDF `3695053.3731100.pdf` (971.82 KB) but exposes **no resolvable bitstream href** (two attempts). | `ABSTRACT_ONLY` — abstract and metadata from the EPFL Infoscience record; authors from the ISCA 2025 census row. | **NO (abstract-level, provisional)** — the abstract states it is a GPU microarchitecture supporting FP8 and Microscaling (MX) formats with "adaptive scaling factors across varying block sizes and multiple scaling hierarchies", i.e. GPU datapath format support. | `CORE_GPU` | Verdict from the abstract: a GPU microarchitecture adding scaled/block numeric-format support to the datapath is squarely cluster F. **Abstract-only ⇒ deep analysis NOT allowed.** On the pending watchlist. | — (watchlist) |
| Virgo: Cluster-level Matrix Unit Integration in GPUs for Scalability and Energy Efficiency | ASPLOS 2025 | `PUBLIC_FULLTEXT` + `PUBLIC_ARTIFACT` — author PDF `people.eecs.berkeley.edu/~ysshao/assets/papers/virgo-asplos2025.pdf`, arXiv 2408.12073, DOI 10.1145/3676641.3716281; artifact `github.com/ucb-bar/virgo` cloned at `dd03a7c87895a937ecbde30daf8aeddb583ec144`. | `FULL_PAPER` + `CODE` — microarchitecture, 3 RTL baselines, Table 2 config, 16nm/400MHz methodology, GEMM + FA3 results, power breakdown, area, limitations, related work. Code: `GemminiTile.scala` MMIO register map, `VirgoSharedMemComponents.scala` bank/subbank logic, `TensorCoreDecoupled.scala` Hopper baseline. `virgo-kernels` NOT inspected. | **NO** — the whole subject is the coupling of matrix operands/accumulators to the per-warp register file; mechanisms are cluster-level matrix unit, dedicated accumulator SRAM, 2-D (bank × subbank) shared-memory banking, unaligned-lane filtering, split read/write paths. | `CORE_GPU` | The only assigned paper that proposes *changing* the matrix-unit integration contract rather than working around it; verified against synthesised RTL and an inspected Chisel artifact. All comparisons are vs the authors' RTL *models* of Volta/Ampere/Hopper, not real silicon. | `GPU-ASPLOS25-01--virgo-cluster-level-matrix-unit.md` |
| Characterizing Matrix Multiplication Units across General Parallel Patterns in Scientific Computing (suite: **Cubie**) | PPoPP 2026 | `PUBLIC_FULLTEXT` — author PDF `ssslab.cn/assets/papers/2026-lu-Cubie.pdf`; DOI 10.1145/3774934.3786456. Artifact `zenodo.org/records/17725527` **NOT_INSPECTED**. | `FULL_PAPER` — §3 suite, §4 quadrant categorisation, §5.2 TC/CC/CC-E variants, §6 performance, §7 power/energy, §8 accuracy, §9 roofline, limitations, related work. | **NO** — the object of study is the matrix unit's fixed tile extents (FP64 `wmma m8n8k4`, bit `mma m8n8k128`) and which input/output tiles each pattern can fill; the CC/CC-E variants are *defined* as swapping the MMA for CUDA-core arithmetic on the same GPU. | `CORE_GPU` | Systematic three-generation (A100/H200/B200) characterisation of MMU benefit across 10 parallel patterns, with a TC/CC/CC-E design that isolates the matrix unit from the algorithmic restructuring. **Census correction: AMD/Intel coverage is NOT in the evaluation — NVIDIA only.** | `GPU-PPoPP26-02--cubie-characterizing-matrix-multiplication-units.md` |
| Accelerating High-Order Finite Element Simulations at Extreme Scale with FP64 Tensor Cores | ISC 2026 (`MEMBERSHIP_UNVERIFIED`) | `PUBLIC_FULLTEXT` — arXiv 2603.09038, HTML v2 (`/abs/` returned empty markdown twice, so the Comments field is unread). **The paper text states no venue** (verified); a "SC '25" attribution seen in a fetch summary is unsupported and rejected. LLNL-PROC-2014338. No artifact (MFEM upstreaming "under review"). | `FULL_PAPER` — §II application/discretisation, §III-A bottleneck, §III-C/D bank-conflict maps, §III-E shape mismatch, §III-F PTX rationale, §III-G fusion, §IV-A GH200 vs GB200, Tables I-VII, scaling, related work, limitations. | **NO** — FP64 `m8n8k4` DMMA warp-cooperative operand sharing ("each element loaded only once among the threads in a warp"), explicit `f_m/f_n/f_k` thread-to-fragment maps with the `[0,2,1,3,4,5,6,7]` bank-conflict permutation, per-lane shared-memory address tables, hand-written PTX over CUTLASS/cuBLAS. | `CORE_GPU` | Uses the FP64 Tensor Core as a *shared-memory-traffic-reduction* device (9,000→1,960 bytes) rather than for arithmetic throughput, accepting `25×5×4`-into-`m8n8k4` padding waste to get it; validated to 9,216 GH200 on Alps. | `GPU-ISC26-02--fp64-tensor-cores-high-order-finite-element.md` |
| FlashSparse: Minimizing Computation Redundancy for Fast Sparse Matrix Multiplications on Tensor Cores | PPoPP 2025 (S8 Tensor Cores) | `PUBLIC_FULLTEXT` — arXiv 2412.11007 HTML. DOI `UNKNOWN` (`dl.acm.org` 403). **No repository URL in the paper** (verified). | `FULL_PAPER` — motivation + Table 2, swap-and-transpose derivation, §3.3 thread mapping, §3.5 ME-BCRS, Figures 1/12(a), evaluation over 515 matrices, per-component ablation, Table 7, related work. **No limitations section.** | **NO** — the asymmetric `m=16` / `n=8` extents of `mma.m16n8k*`; the swap `A×B=(BᵀAᵀ)ᵀ` halves sparse blocking to 8×1 and removes 43% of MMA invocations; a column shuffle making 8 lanes coalesce into one 32-byte transaction. | `CORE_GPU` | Pure exploitation of MMA operand-slot asymmetry plus a storage format defined relative to the instruction's `k` extent. **Uses dense MMA on denser 8×8 blocks — NOT Sparse Tensor Cores / 2:4.** | `GPU-PPoPP25-01--flashsparse-swap-and-transpose-sparse-tensor-cores.md` |
| Acc-SpMM: Accelerating General-purpose Sparse Matrix-Matrix Multiplication with GPU Tensor Cores | PPoPP 2025 (S8 Tensor Cores) | `PUBLIC_FULLTEXT` — arXiv 2501.09251 HTML. DOI `UNKNOWN` (`dl.acm.org` 403). **No repository URL in the paper** (verified). | `FULL_PAPER` — three obstacles, §3.1 four components, BitTCF array layout + storage model, `cp.async` double-buffer pipeline, IBD load balancing, evaluation on 3 GPUs / 424 matrices, per-component ablation (**qualitative only**), future work, related work. | **NO** — `TCLocalBit` is one `uint64` because an 8×8 TC block has exactly 64 positions, and the storage model `(⌈M/8⌉ + NumTCBlock×11 + 2)×4` bytes is written in the instruction's 8-row window; "swapped mma" for 8×8 partitioning; Ampere-and-later `cp.async` + `WaitGroup()` double-buffered against warp MMA issue. | `CORE_GPU` | Four-component general-purpose TC SpMM whose format and pipeline are defined by the MMA tile size and `cp.async` semantics. Speedup over cuSPARSE *falls* with newer higher-bandwidth parts (2.52× RTX4090 → 1.91× A800 → 1.58× H100). Ablation is not numerically separated. | `GPU-PPoPP25-02--acc-spmm-general-purpose-tensor-core-spmm.md` |

---

## 2. Verdict-only papers

All rows below were resolved from census evidence (confirmed titles, DOIs,
official program pages, artifact URLs) plus, where noted, the citation
evidence recovered from the priority papers' own related-work sections.
**No row in this section may be used to justify a deep analysis.**

| Paper | Venue/Year | Access state | Evidence read | Counterfactual answer | Verdict | Rationale (1-2 sentences) | Deep analysis file |
|---|---|---|---|---|---|---|---|
| AmgT: Algebraic Multigrid Solver on Tensor Cores | SC 2024 | `PUBLIC_ARTIFACT_ONLY` — DOI 10.1109/SC41406.2024.00058, `ieeexplore` 418, no arXiv found; artifact `github.com/SuperScientificSoftwareLaboratory/AmgT`. | `TITLE_AND_CENSUS_ROW` + **corroborating citation evidence**: the PPoPP'26 Cubie paper uses "AmgT-SpGEMM [53]" as its SpGEMM baseline, confirming AmgT is a Tensor-Core SpGEMM/SpMV implementation. | **NO (title/census-level, provisional)** — AMG's SpGEMM/SpMV inner kernels mapped onto Tensor Cores. | `CORE_GPU` | Multigrid solver whose Galerkin-product and smoother kernels are recast for the GPU matrix unit; confirmed as a TC kernel by an independent paper's baseline choice. | — (watchlist; artifact-only ⇒ no deep analysis) |
| LoRAStencil: Low-Rank Adaptation of Stencil Computation on Tensor Cores | SC 2024 | `PUBLIC_FULLTEXT` — author PDF `likun.tech/pdf/sc24_lorastencil.pdf`; DOI 10.1109/SC41406.2024.00059; artifact `github.com/zondie17/LoRAStencil`. **Not fetched in this pass (capacity).** | `TITLE_AND_CENSUS_ROW` + citation evidence: SPIDER cites it as [49] (low-rank decomposition leveraging symmetry) and measures it at ≈2.94× theoretical-minimum computation for Box-2D3R; Cubie uses it as the stencil baseline [101]. | **NO (provisional)** — low-rank factorisation chosen to fit the matrix-unit fragment shape for stencils. | `CORE_GPU` | Third member of the TC-stencil line (TCStencil → ConvStencil → LoRAStencil → FlashFFTStencil → SparStencil/SPIDER); public full text exists and it is the **highest-value remaining deep-analysis candidate** in this cluster. | — (watchlist: full text available, not yet read) |
| High Performance Unstructured SpMM Computation Using Tensor Cores | SC 2024 | `PUBLIC_FULLTEXT` — arXiv 2408.11551 and author PDF `unixer.de/publications/img/okanovic-sc24-high.pdf`; DOI 10.1109/SC41406.2024.00060. **Not fetched (capacity).** | `TITLE_AND_CENSUS_ROW` (Patrik Okanovic et al.). | **NO (provisional)** — unstructured sparsity mapped onto dense/sparse TC fragment shapes. | `CORE_GPU` | Direct precursor to the FlashSparse / Acc-SpMM / "Bridging the Gap" TC-SpMM line; public full text available. | — (watchlist: full text available, not yet read) |
| Mille-feuille: A Tile-Grained Mixed Precision Single-Kernel Conjugate Gradient Solver on GPUs | SC 2024 | `PUBLIC_FULLTEXT` — author PDF `ssslab.cn/assets/papers/2024-yang-Millefeuille-final.pdf` read; DOI 10.1109/SC41406.2024.00064; artifact `github.com/SuperScientificSoftwareLaboratory/Mille-feuille` not inspected. | `FULL_PAPER` — see the authoritative sparse/irregular ledger row and deep analysis. | **NO** — persistent-kernel grid synchronization and shared-memory residency are GPU-specific, although no Tensor Core is involved. | `CORE_GPU` | **Corrected by `_LEDGER_sparse_irregular.md:48` and `GPU-SC24-103--mille-feuille-tile-grained-mixed-precision-single-kernel-cg.md`.** It remains here only as a negative Tensor-Core boundary case. | `GPU-SC24-103--mille-feuille-tile-grained-mixed-precision-single-kernel-cg.md` |
| M3XU: Achieving High-Precision and Complex Matrix Multiplication with Low-Precision MXUs | SC 2024 | `CLOSED_ACCESS` — DOI 10.1109/SC41406.2024.00016, `ieeexplore` 418; no preprint located. | `TITLE_AND_CENSUS_ROW`. | **NO (provisional)** — high-precision and complex GEMM reconstructed from low-precision matrix-unit (MXU) products. | `CORE_GPU` | The earliest item in this cluster's precision-emulation line and a direct precursor to the SC 2026 Ozaki-II/EmuGEMM pair (both of which also handle *complex* arithmetic). **"MXU" may denote a non-GPU matrix unit — GPU centrality `UNVERIFIED`.** | — (watchlist) |
| MXBLAS: Accelerating 8-bit Deep Learning with a Unified Micro-Scaled GEMM Library | SC 2025 | `PENDING_FULLTEXT` — DOI 10.1145/3712285.3759809; census observed an ACM `doi/pdf` URL form (`ACM_OA?`, licence unconfirmed) but `dl.acm.org` returns 403 here. | `TITLE_AND_CENSUS_ROW` (Weihu Wang et al.). | **NO (provisional)** — micro-scaled (MX) FP8 block-format GEMM on GPU Tensor Cores; MX scale-factor handling is a matrix-unit format concern. | `CORE_GPU` | Library-level counterpart to Avant-Garde's microarchitectural MX support; sits on the *numeric-format* branch of this cluster. Plausibly also in the un-imported AI/HPC corpus — `KNOWN_EXTERNAL_CORPUS_NOT_IMPORTED`. | — (watchlist) |
| Bridging the Gap between Unstructured SpMM and Structured Sparse Tensor Cores | SC 2025 | `CLOSED_ACCESS` — DOI 10.1145/3712285.3759849 (pp. 645-660), `dl.acm.org` 403, no arXiv found. | `TITLE_AND_CENSUS_ROW`. | **NO (provisional)** — mapping unstructured sparsity onto the 2:4 Sparse Tensor Core metadata contract. | `CORE_GPU` | Same mechanism family as SPIDER (`GPU-PPoPP26-01`) and SparStencil but for SpMM rather than stencils; the unstructured→2:4 bridging problem is definitionally about SpTC metadata. | — (watchlist) |
| SparStencil: Retargeting Sparse Tensor Cores to Scientific Stencil Computations via Structured Sparsity Transformation | SC 2025 | `PENDING_FULLTEXT` — DOI 10.1145/3712285.3759820, `dl.acm.org` 403; a Semantic Scholar record for it was surfaced in search but not fetched. | `TITLE_AND_CENSUS_ROW`. | **NO (provisional)** — stencils retargeted to Sparse Tensor Cores via a structured-sparsity transformation. | `CORE_GPU` | **Priority conflict flagged**: this is the *same problem and same mechanism* as SPIDER/SPTCStencil (`GPU-PPoPP26-01`) one venue cycle earlier, and the SPTCStencil text read does **not** cite it while claiming to be "the first to harness SpTCs for acceleration beyond deep learning domains". Priority/independence `UNRESOLVED` — needs SparStencil's full text. | — (watchlist; **highest-priority access target**) |
| LiquidGEMM: Hardware-Efficient W4A8 GEMM Kernel for High-Performance LLM Serving | SC 2025 | `PENDING_FULLTEXT` — DOI 10.1145/3712285.3759852, `dl.acm.org` 403. | `TITLE_AND_CENSUS_ROW`. | **NO (provisional)** — W4A8 quantised GEMM executed on Tensor Cores; the 4-bit weight dequantisation must be arranged to match the MMA operand layout. | `RELATED_GPU` | A quantised-GEMM serving kernel: genuinely a Tensor Core kernel, but its research contribution is LLM-serving throughput rather than a matrix-unit or numeric-format mechanism. Border case; `RELATED_GPU` pending full text. Plausibly in the un-imported AI/HPC corpus — `KNOWN_EXTERNAL_CORPUS_NOT_IMPORTED`. | — (watchlist) |
| T2-RELION: Task Parallelism, Tensor Core Accelerated RELION for Cryo-EM 3D Reconstruction | SC 2025 | `PENDING_FULLTEXT` — DOI 10.1145/3712285.3759824, `dl.acm.org` 403. | `TITLE_AND_CENSUS_ROW`. | **PROVISIONAL / UNRESOLVED** — the title names both task parallelism and Tensor Core acceleration; whether the TC part is a mechanism contribution or a GEMM-on-TC port cannot be told from the title. | `UNRESOLVED` | Applying Tensor Cores to cryo-EM reconstruction is in scope *if* the mapping is non-trivial; **"we ran a GEMM on Tensor Cores and got a speedup" would not qualify.** Verdict deliberately withheld pending the paper. | — (watchlist) |
| Do We Need Tensor Cores for Stencil Computations? | SC 2026 | `CLOSED_ACCESS` — official SC26 Best Student Paper nominee page `sc26.supercomputing.org/2026/08/announcing-best-paper-and-best-student-paper-finalists/`; no arXiv found. | `TITLE_AND_CENSUS_ROW` — authors Qiqi Gu, Haibing Guan, Chenpeng Wu, Jianguo Yao, Heng Shi; **`CONFIRMED_IN_POPULATION`** (official nominee). | **NO (provisional)** — the paper's subject is the utility of the GPU matrix unit for stencils, i.e. it interrogates a matrix-unit mechanism directly. | `CORE_GPU` | By the **same SJTU group as SPIDER/SPTCStencil** (`GPU-PPoPP26-01`), and it interrogates the premise of the entire TC-stencil line this cluster documents. **The single most important watchlist item for the cluster's synthesis**; no public full text. | — (watchlist; **highest research value**) |
| Pushing the Limits of Structured Sparse GEMM on Hopper GPUs | SC 2026 (seed) | `ABSTRACT_ONLY` → in fact **`NOT_FOUND`** | **NOT_FOUND_AFTER_SEARCH** — two independent searches (census pass + one here) located no paper with this title. The nearest hits were unrelated: *SlideSparse* (arXiv 2603.05232, "(2N-2):2N model family on commodity GPUs", A100/H100/B200) and 2:4-sparsity work at other venues. SlideSparse is **not** this title. | n/a | `UNRESOLVED` | **Existence unverified.** The SC 2026 population is not fully enumerated in the census, so absence is weak evidence, but no source supports this title. Treat as a possibly-mistaken seed entry until an official SC26 program listing is reachable. | — |
| FlashFFTStencil: Bridging Fast Fourier Transforms to Memory-Efficient Stencil Computations on Tensor Core Units | PPoPP 2025 (S8 Tensor Cores) | `CLOSED_ACCESS` — official page `ppopp25.sigplan.org/details/PPoPP-2025-Main-Conference-1/47/x`; DOI `UNKNOWN`; no arXiv, no artifact found. | `TITLE_AND_CENSUS_ROW` (Haozhi Han et al.) + citation evidence: SPIDER cites it as [19] (FFT-based, enhancing arithmetic intensity) and measures itself 1.35× faster than it; Cubie cites it as [28]. | **NO (provisional)** — FFT route to stencils explicitly targeted at Tensor Core Units; the arithmetic-intensity argument is about filling the matrix unit. | `CORE_GPU` | Fourth member of the TC-stencil line; the strongest baseline SPIDER reports (only 1.35× behind), so it is the current TC-stencil performance reference point. | — (watchlist) |
| BerryBees: Breadth First Search by Bit-Tensor-Cores | PPoPP 2025 (S8 Tensor Cores) | `CLOSED_ACCESS` — official page `ppopp25.sigplan.org/details/PPoPP-2025-Main-Conference-1/11/x`; DOI `UNKNOWN`; no arXiv, no artifact found. | `TITLE_AND_CENSUS_ROW` (Yuyao Niu, Marc Casas) + citation evidence: Cubie uses BerryBees [59] as its BFS baseline and reports BFS at 2.6-3.0× via the **bit** `mma m8n8k128` instruction. | **NO** — the **bit/binary** MMA path (`mma.m8n8k128.b1` family), a distinct matrix-unit datapath with XOR-popcount semantics, applied to graph traversal. | `CORE_GPU` | The clearest case in this cluster of a matrix unit used for a genuinely non-arithmetic kernel: BFS frontier expansion as Boolean matrix products on bit tensor cores. Co-author Marc Casas also co-authors Cubie. | — (watchlist) |
| Accelerating GNNs on GPU Sparse Tensor Cores through N:M Sparsity-Oriented Graph Reordering | PPoPP 2025 (S1 GNN) | `CLOSED_ACCESS` — official page `ppopp25.sigplan.org/details/PPoPP-2025-Main-Conference-1/32/x`; DOI `UNKNOWN`; no arXiv, no artifact found. | `TITLE_AND_CENSUS_ROW` (Jou-An Chen et al.); census notes the abstract names **NVIDIA A100 Sparse Tensor Cores** and N:M sparsity mapping. | **NO (provisional)** — graph reordering whose objective function is conformance to the SpTC's N:M (2:4) metadata pattern. | `CORE_GPU` | Reordering-to-fit-the-metadata-contract is the same family as SPIDER's strided swapping and "Bridging the Gap"; Ampere SpTC is named in the abstract. | — (watchlist) |
| Exploiting Efficient Mapping and Pipelined Execution for Accelerating SpMV on Tensor Cores | PPoPP 2026 | `CLOSED_ACCESS` — official page `ppopp26.sigplan.org/details/PPoPP-2026-papers/31/x`; DOI 10.1145/3774934.3786441; no arXiv, no artifact found. | `TITLE_AND_CENSUS_ROW` (Kaige Zhang et al.). | **NO (provisional)** — mapping (fragment/tile assignment) plus a software pipeline for SpMV on Tensor Cores. | `CORE_GPU` | Direct successor to Acc-SpMM's mapping+pipeline pair (`GPU-PPoPP25-02`) applied to SpMV rather than SpMM. Note Cubie independently finds SpMV is the one Quadrant-IV pattern where removing the MMU-induced redundancy *improves* performance (CC-E 1.0-1.2× vs TC) — a useful check on this paper's premise when it becomes readable. | — (watchlist) |
| FlashAttention-T: Towards Fully Tensorized Attention by Exploiting Tensor-Vector Parallelism | PPoPP 2026 | `PUBLIC_ARTIFACT_ONLY` — official page `ppopp26.sigplan.org/details/PPoPP-2026-papers/15/x`; DOI 10.1145/3774934.3786425; artifact `zenodo.org/records/17673796`; no preprint found. | `TITLE_AND_CENSUS_ROW` (Jianxing Xu et al.); census notes the abstract names **NVIDIA Ampere (A100, AGX Orin), Hopper (H100), Tensor Cores and CUDA cores**. | **NO (provisional)** — co-scheduling of the Tensor Core and the CUDA-core (vector) pipes, i.e. matrix-unit/vector-unit concurrency on the same SM. | `CORE_GPU` | "Tensor-vector parallelism" is a matrix-unit *scheduling* mechanism and is in scope; it is the same resource-concurrency question Virgo answers architecturally. Plausibly in the un-imported AI/HPC corpus — `KNOWN_EXTERNAL_CORPUS_NOT_IMPORTED`. | — (watchlist) |
| Cooperative Warp Execution in Tensor Core for RISC-V GPGPU | HPCA 2025 | `CLOSED_ACCESS` — official program `hpca-conf.org/2025/program/main-program/`; `ieeexplore` 418; no arXiv, no artifact found. | `TITLE_AND_CENSUS_ROW` (Abubakr Nada et al., session "10B: All in on GPUs - 1"). | **NO (provisional)** — warp-level cooperative issue into a Tensor Core datapath inside an open RISC-V GPGPU. | `CORE_GPU` | Closest neighbour to Virgo (`GPU-ASPLOS25-01`): both add matrix units to an open RISC-V GPU, but this attacks *warp-cooperative issue* where Virgo attacks *cluster-level disaggregation*. Complementary, not duplicate. | — (watchlist) |
| Uni-STC: Unified Sparse Tensor Core | HPCA 2026 | `CLOSED_ACCESS` — official track page `2026.hpca-conf.org/track/hpca-2026-main-conference`; no arXiv, no artifact found. | `TITLE_AND_CENSUS_ROW` (Haocheng Lian et al.); census flags GPU integration as unconfirmed. | **NO (provisional)** — a Sparse Tensor Core datapath design, i.e. the unit itself is the contribution. | `CORE_GPU` | Architecture-side counterpart to the software SpTC papers (SPIDER, Bridging the Gap, N:M reordering): generalising the 2:4-only metadata contract that all of them have to work around. **Cubie cites Uni-STC in its future-work discussion of sparse-specialised tensor cores**, corroborating its relevance. GPU-integration `UNVERIFIED`. | — (watchlist) |
| BitDecoding: Unlocking Tensor Cores for Long-Context LLMs with Low-Bit KV Cache | HPCA 2026 | `PUBLIC_FULLTEXT` — arXiv 2503.18773 (not fetched in this pass; capacity). Official program `2026.hpca-conf.org/program/program-hpca-2026/`. | `TITLE_AND_CENSUS_ROW` (Dayou Du et al.). | **NO (provisional)** — low-bit KV-cache layouts arranged so the dequantised tiles feed Tensor Core MMA operands. | `RELATED_GPU` | Genuinely a Tensor Core kernel paper, but the contribution is LLM decoding throughput via KV-cache quantisation rather than a matrix-unit or numeric-format mechanism. Border case. Plausibly in the un-imported AI/HPC corpus — `KNOWN_EXTERNAL_CORPUS_NOT_IMPORTED`; **asserting nothing about duplication.** | — (watchlist) |
| LUT Tensor Core: A Software-Hardware Co-Design for LUT-Based Low-Bit LLM Inference | ISCA 2025 | `PUBLIC_FULLTEXT` — arXiv 2408.06003 (not fetched; capacity). DOI 10.1145/3695053.3731057. | `TITLE_AND_CENSUS_ROW`; census **FLAGs** that the arXiv abstract describes "a LUT-based Tensor Core hardware design" for mpGEMM, pointing to a custom unit rather than an NVIDIA-GPU integration. | **NO (provisional)** — replaces the multiplier array of a matrix unit with a lookup table for mixed-precision GEMM. | `UNRESOLVED` | Genuinely ambiguous, as the census flagged: if the LUT unit is a *GPU* Tensor Core replacement it is `CORE_GPU`; if it is a standalone accelerator it is out of cluster. **Verdict deliberately withheld**; full text is public and this is a cheap resolution. | — (watchlist; cheap to resolve) |
| Neo: Towards Efficient Fully Homomorphic Encryption Acceleration using Tensor Core | ISCA 2025 | `CLOSED_ACCESS` — DOI 10.1145/3695053.3731408, `dl.acm.org` 403; no preprint found. | `TITLE_AND_CENSUS_ROW`; census flags `ABSTRACT_NOT_RETRIEVED` and that GPU-vs-custom-unit was not established. | **PROVISIONAL** — FHE's NTT/polynomial-multiplication inner kernels mapped onto a matrix unit would be a genuine non-GEMM matrix-unit application. | `UNRESOLVED` | In-scope *if* these are GPU Tensor Cores; the census could not establish that. Note Cubie cites "Accelerating FFT & NTT with tensor cores [23, 75]", so the NTT-on-TC mechanism class is real. Verdict withheld. | — (watchlist) |
| MXFFP: Microscaling Flexible Floating Point Format for Large-Scale AI Model Acceleration | ISCA 2026 | `CLOSED_ACCESS` — DOI 10.1109/ISCA66397.2026.00175, `ieeexplore` 418; no preprint found. | `TITLE_AND_CENSUS_ROW` — authors Sungwoo Kim, Sungbin Kim, Dongho Ha, Hyunwuk Lee, Junsung Kim, Seunghyun Lee, Mingu Jung, Murali Annavaram, Won Woo Ro; census flags GPU centrality `UNVERIFIED`. | **PROVISIONAL** — a microscaling (MX-family) numeric-format datapath. | `UNRESOLVED` | The census's own note is the right one: **Dongho Ha and Won Woo Ro also co-author Avant-Garde (ISCA 2025), so a GPU context is *likely* — but that is an inference about the authors, not evidence about this paper.** Verdict withheld; if GPU-integrated it is the direct successor to Avant-Garde on the numeric-format branch. | — (watchlist) |
| Coruscant: Co-Designing GPU Kernel and Sparse Tensor Core to Advocate Unstructured Sparsity in Efficient LLM Inference | MICRO 2025 | `PUBLIC_FULLTEXT (nominally)` / `PENDING_FULLTEXT (from here)` + `PUBLIC_ARTIFACT` — DOI 10.1145/3725843.3756065; the census observed that ACM DL renders a `/doi/full/` HTML view, but `dl.acm.org` returns 403 here; artifact `github.com/dhjoo98/coruscant`. | `TITLE_AND_CENSUS_ROW` (Donghyeon Joo, Helya Hosseini, Ramyad Hadidi, Bahar Asgari). | **NO (provisional)** — explicit GPU-kernel / Sparse-Tensor-Core **co-design** for unstructured sparsity. | `CORE_GPU` | The title states the co-design of a GPU kernel *and* the Sparse Tensor Core, which is definitionally in cluster F; it is the architecture+kernel counterpart to the software-only SpTC papers. Artifact is public and un-inspected. | — (watchlist; artifact available) |
| DynSpAttn: Efficient Attention via Dual-Side Dynamic Sparsity on Sparse Tensor Cores | ICS 2026 | `CLOSED_ACCESS` — census row from the ICS 2026 program; DOI `UNKNOWN`; full text `NOT_SEARCHED`. | `TITLE_AND_CENSUS_ROW` (X. Yu et al., session "S21 AI Kernels & Parallelism"). | **NO (provisional)** — dual-side (both operands) dynamic sparsity on the Ampere+ 2:4 Sparse Tensor Core; "dual-side" is notable because the SpTC contract sparsifies only one operand. | `RELATED_GPU` | The SpTC metadata mechanism is named, so the mechanism is in scope, but the contribution is attention-kernel efficiency. Border case; **"dual-side" on a single-side-sparse unit would be a real mechanism contribution** and could upgrade to `CORE_GPU` on reading. | — (watchlist) |
| BLEST: Blazingly Efficient BFS using Tensor Cores | ICS 2026 | `CLOSED_ACCESS` — census row from the ICS 2026 program; DOI `UNKNOWN`; full text `NOT_SEARCHED`. | `TITLE_AND_CENSUS_ROW` (D. Elbek, K. Kaya, session "S13 Graph Traversal & Connectivity"). | **NO (provisional)** — BFS expressed as matrix products on Tensor Cores. | `CORE_GPU` | Direct successor to BerryBees (PPoPP 2025) in the graph-traversal-on-matrix-unit line; whether it uses the bit MMA path or a numeric one is unknown. | — (watchlist) |
| EPIClear: Exploiting Domain-Specific Features for Epistasis Detection Acceleration on Tensor Cores | ICS 2025 | `CLOSED_ACCESS` — census row from the ICS 2025 program; DOI `UNKNOWN`; full text `NOT_SEARCHED`. | `TITLE_AND_CENSUS_ROW` (Ricardo Nobre et al., session "Applications"). | **PROVISIONAL** — epistasis detection is a contingency-table/popcount workload, so a bit-MMA mapping would be a genuine non-GEMM matrix-unit use. | `CORE_GPU` | Same mechanism class as BerryBees/BLEST (bit-level operations recast as matrix products) applied to genotype interaction search; in scope as a non-GEMM matrix-unit application. Verdict provisional on the title + the known shape of the workload. | — (watchlist) |
| Fused3S: Fast Sparse Attention on Tensor Cores | ICS 2025 | `CLOSED_ACCESS` — census row from the ICS 2025 program; DOI `UNKNOWN`; full text `NOT_SEARCHED`. | `TITLE_AND_CENSUS_ROW` (Zitong Li, Aparna Chandramowlishwaran, session "Graph Neural Networks"). | **NO (provisional)** — fusing the three sparse stages (SDDMM / softmax / SpMM) into one Tensor Core kernel, i.e. keeping intermediates in matrix-unit accumulators. | `RELATED_GPU` | The fusion-to-avoid-global-memory-round-trips mechanism is the same one EmuGEMM (`GPU-SC26-02`) uses for emulation, so it is mechanistically in scope; the application is sparse attention/GNN. Border case. | — (watchlist) |
| The Tensor-Core Beamformer: A High-Speed Signal-Processing Library for Multidisciplinary Use | IPDPS 2025 | `CLOSED_ACCESS` — census row from `ipdps.org/ipdps2025/2025-advance-program.html`; DOI `UNKNOWN`; no full text or artifact found. | `TITLE_AND_CENSUS_ROW` (session "S14 Data and Signal Processing"). | **NO (provisional)** — radio-astronomy beamforming (a complex-valued correlation/sum) mapped onto Tensor Core fragments. | `CORE_GPU` | A non-GEMM scientific kernel (beamforming) recast for the matrix unit, delivered as a library — the same category as the FEM and stencil papers. `[inference]` complex-valued beamforming on real-valued MMA requires an explicit real/imaginary fragment decomposition, which would be a genuine mechanism; unverified. | — (watchlist) |
| BRP-SpMM: Block-Row Partition Based Sparse Matrix Multiplication with Tensor and CUDA Cores | IPDPS 2025 | `CLOSED_ACCESS` — census row from `ipdps.org/ipdps2025/2025-advance-program.html`; DOI `UNKNOWN`; no full text or artifact found. | `TITLE_AND_CENSUS_ROW` (session "S23 Matrix Multiplication"). | **NO (provisional)** — SpMM partitioned across *two* GPU execution units (Tensor Cores and CUDA cores) by block-row density. | `CORE_GPU` | Explicitly a matrix-unit/vector-unit work-partitioning mechanism, same family as FlashAttention-T's "tensor-vector parallelism"; and it is a direct contemporary of FlashSparse and Acc-SpMM in the TC-SpMM line. | — (watchlist) |
| Scalable Hamming Distance Computation Using Accelerated Matrix Transformations | ISC 2025 | `PENDING_FULLTEXT` — census records the official ISC statement that ISC 2025 papers are open access at IEEE Xplore, but `ieeexplore` returns 418 here and the per-paper URL was not retrieved. Official session page `isc.app.swapcard.com/...`. | `TITLE_AND_CENSUS_ROW` — Rabab Alomairy, Qinglei Cao, Hatem Ltaief, Alan Edelman (presenter David Keyes, KAUST). Census flags GPU centrality `UNVERIFIED` and the row as `[TITLE-ONLY]`. | **PROVISIONAL** — Hamming distance is XOR+popcount, so the natural mapping is the bit MMA path; "accelerated matrix transformations" suggests a matrix-engine recasting. | `UNRESOLVED` | Mechanism class fits the cluster (bit/integer matrix unit for a non-GEMM kernel) but **neither the GPU nor the matrix unit is named in the title**, and no full text was read. Verdict withheld rather than guessed. | — (watchlist) |
| Complex Tensor Core (seed: "Software-Hardware Co-Design for Accelerating Complex-Valued Neural Networks on GPUs") | MICRO 2026 (seed) | **`NOT_FOUND`** | **NOT_FOUND_AFTER_SEARCH** — the census recorded that searches for this exact title returned only MICRO-52's "Sparse Tensor Core" and vendor Tensor Core material. Not re-searched here (quota); the census's negative result stands. | n/a | `UNRESOLVED` | **Existence unverified.** Note that *complex* matrix multiplication on integer matrix units is a real and active thread in this cluster — M3XU (SC 2024), the Ozaki-II artifact's complex modulus tables, and EmuGEMM's CRT-based 3M complex GEMM — so a paper of this kind is plausible, but no source supports this title. | — |

---

## 3. Access-state summary

| Access state | Count | Papers |
|---|---|---|
| `PUBLIC_FULLTEXT` (read) | 10 | ConvStencil, SPIDER/SPTCStencil, Ozaki-II FP8, EmuGEMM, Virgo, Cubie, FP64-TC FEM, FlashSparse, Acc-SpMM, Mille-feuille |
| `PUBLIC_FULLTEXT` (available, not read — capacity) | 4 | LoRAStencil, High Performance Unstructured SpMM, BitDecoding, LUT Tensor Core |
| `PENDING_FULLTEXT` (open access but unreachable from here) | 6 | Avant-Garde, MXBLAS, SparStencil, LiquidGEMM, Coruscant, Scalable Hamming Distance |
| `PUBLIC_ARTIFACT_ONLY` | 2 | AmgT, FlashAttention-T |
| `CLOSED_ACCESS` | 18 | M3XU, Bridging the Gap, T2-RELION, Do We Need Tensor Cores, FlashFFTStencil, BerryBees, N:M Graph Reordering, SpMV on Tensor Cores, Cooperative Warp Execution, Uni-STC, Neo (FHE), MXFFP, DynSpAttn, BLEST, EPIClear, Fused3S, Tensor-Core Beamformer, BRP-SpMM |
| `NOT_FOUND` (existence unverified) | 2 | Pushing the Limits of Structured Sparse GEMM on Hopper GPUs; Complex Tensor Core |

## 4. Verdict summary

| Verdict | Count |
|---|---|
| `CORE_GPU` | 31 |
| `RELATED_GPU` | 4 (LiquidGEMM, BitDecoding, DynSpAttn, Fused3S) |
| `UNRESOLVED` | 7 (T2-RELION, Pushing the Limits…, LUT Tensor Core, Neo (FHE), MXFFP, Scalable Hamming Distance, Complex Tensor Core) |
| `EXCLUDE` | 0 |
| **total** | **42** (10 priority + 32 verdict-only) |

**No paper in this cluster was excluded.** Cluster F's papers self-select
strongly: naming a Tensor Core / Matrix Core / MXU / Sparse Tensor Core /
microscaling format in a title is a reliable indicator of a GPU matrix-unit
mechanism. The five `RELATED_GPU` calls are all *application*-led papers
(LLM serving, KV cache, attention, a CG solver) that use the matrix unit
without contributing a matrix-unit mechanism; the seven `UNRESOLVED` calls
are split between unverifiable titles (2) and papers where GPU-vs-custom-unit
could not be established from the title alone (5).

## 5. Stable IDs assigned by this cluster

`GPU-PPoPP24-01`, `GPU-PPoPP25-01`, `GPU-PPoPP25-02`, `GPU-PPoPP26-01`,
`GPU-PPoPP26-02`, `GPU-ASPLOS25-01`, `GPU-SC26-01`, `GPU-SC26-02`,
`GPU-ISC26-02`.

**Known collision for central reconciliation:** a parallel cluster has also
written `GPU-ISC26-02--pico-performance-insights-collective-operations.md`
into this directory. The two files do not overwrite each other (different
slugs) but the stable ID `GPU-ISC26-02` is duplicated and one of the two must
be renumbered centrally. Also present from other clusters and noted only for
awareness: `GPU-SC24-01` appears twice (`gpu-to-gpu-communication…` and
`hirace…`), and `GPU-ASPLOS25-01` is used only by this cluster (Virgo).
