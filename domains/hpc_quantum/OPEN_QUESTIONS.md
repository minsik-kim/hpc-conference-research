# OPEN_QUESTIONS — HPC & Quantum

Status: **imported source's own candidates indexed, none re-falsified**

This domain's imported source project does not contain a standalone
gap/novelty document — its `VENUE_GAP`, `POSSIBLE_CROSSOVER`, and
`OPEN_QUESTION` observations are used inline, across two censuses and a
lineage synthesis document. They are indexed with exact pointers in
`research/CANDIDATE_QUESTIONS.md` (10 `VENUE_GAP`, 5 `POSSIBLE_CROSSOVER`,
3 `OPEN_QUESTION` entries).

**Every entry there is `CANDIDATE` status only.** None has passed through
the full `governance/RESEARCH_GAP_RULES.md` pipeline (own-corpus
cross-check → closest-work search → external targeted literature search →
novelty falsification → experiment feasibility) during this import — doing
so would be new literature research, which this import step explicitly
does not perform.

**The single most load-bearing candidate**, per the source's own framing:
real-time QEC decoding is structurally a classical HPC problem (latency-
and throughput-bounded, parallelizable, with a hard cryogenic-boundary
bandwidth constraint) and is the densest topic at four architecture
venues, yet is absent from SC's main track in both censused years — see
`research/CANDIDATE_QUESTIONS.md` entry G1. The source is explicit that
this is a `VENUE_GAP`, not a claimed research gap.

Do not generate new candidate research gaps for this domain from general
knowledge. Do not promote any entry in `research/CANDIDATE_QUESTIONS.md`
above `CANDIDATE` without running it through the governance pipeline.
